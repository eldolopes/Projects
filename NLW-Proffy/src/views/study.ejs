{% include "partials/header.ejs" %}

<body id="page-study">
    <div id="container">
        <header class="page-header">
            <div class="top-bar-container">
                <a href="/">
                <img src="/images/icons/back.svg" alt="Voltar">
                </a>            
                <img src="/images/logo.svg" alt="Proffy">
            </div>           
            <div class="header-content">   
                <!--
                <p>{{ allTeachers }}</p> 
                <p>{{ allFilters.subject }}</p>
                <p>{{ allCourses.courses }}</p>
                -->            
                <strong>Esses são os proffys disponíveis</strong> 
                
                <h1 style="color: red">{{ allClasses }}</h1>          
                <h1 style="color: red">{{ allProffys }}</h1> 
                <form id="search-teachers">                    
                    <div class="select-block">
                        <label for="subject" id="subject">Matéria</label>  
                        <select name="subject" id="subject">
                            <option value="" disabled="" hidden="">Selecione uma opção</option> 
                            {%for courses in allCourses %}                           
                            <option value="{{loop.index}}" {{ "selected" if allFilters.subject ==  loop.index }}>
                                {{courses}}
                            </option>
                            {%endfor%}                                                                      
                        </select>  
                    </div>
                    <div class="select-block">
                        <label for="weekday" id="subject">Dia da Semana</label>  
                        <select name="weekday" id="weekday">
                            <option value="" disabled="" hidden="">Selecione uma opção</option>
                            {%for days in weekdays %}
                            <option value="{{loop.index0}}" {{ "selected" if allFilters.weekday == loop.index0 }}>
                                {{days}}
                            </option>
                            {%endfor%}                            
                        </select>  
                    </div>
                    <div class="input-block">
                        <label for="time">Horário</label>  
                        <input name="time" id="time" type="time" value="{{allFilters.time}}"> 
                    </div>
                    <button type="submit">Filtrar</button>
                </form>                
            </div>
        </header>
      
        <main>
            {%for proffy in allProffys %}
            <article class="teacher-item">                
                <header>                    
                    <img 
                        src="{{ proffy.avatar }}" 
                        alt="{{ proffy.name }}">
                    <div>
                        <strong>{{proffy.name }}</strong>
                        <span>{{ allClasses[0].subject }}</span>
                    </div>
                </header>            
                <p>{{ proffy.bio }}</p>            
                <footer>
                    <p>Preço/hora<strong>R$ {{ allClasses[0].cost }},00</strong></p>
                    <a class="button" target="_blank" style="text-decoration:none" 
                    href="https://api.whatsapp.com/send?1=pt_BR&phone=55{{ proffy.whatsapp }}&text=Olá, {{ proffy.name }}. Tenho interesse na sua aula de {{ allClasses[0].subject }} anunciada no Proffy.">
                        <img src="/images/icons/whatsapp.svg" alt="Whatsapp">Entrar em contato
                    </a> 
                </footer>                
            </article>  
            {%endfor%}
            
                
        </main>
    </div> 

</body>
</html>